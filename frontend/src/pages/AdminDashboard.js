import React from 'react';
import Dashboard from './Dashboard';  // Import the existing Dashboard component

const AdminDashboard = () => {
  return <Dashboard />;  // Render the Dashboard component
};

export default AdminDashboard;
